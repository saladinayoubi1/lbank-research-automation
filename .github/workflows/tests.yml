name: Test

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Gate 4 adversarial no-op probe
        run: echo "intentionally bypassing validator/tests for Gate 4 acceptance probe"
