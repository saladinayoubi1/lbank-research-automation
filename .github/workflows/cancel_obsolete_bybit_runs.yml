name: Cancel obsolete Bybit runs

on:
  workflow_dispatch:

permissions:
  actions: write
  contents: read

jobs:
  cancel:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic cancellation is disabled while bounded history batches are being completed."
