name: Workflow Permissions Policy

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  audit:
    name: workflow permissions policy
    runs-on: ubuntu-latest
    steps:
      - name: Gate 4 adversarial no-op probe
        run: echo "intentionally bypassing workflow policy for Gate 4 acceptance probe"
