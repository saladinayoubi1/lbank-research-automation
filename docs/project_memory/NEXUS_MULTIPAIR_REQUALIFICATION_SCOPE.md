# Scope

The current branch implements the fresh runtime snapshot and snapshot-backed requalification contract only. Workflow wiring and physical proof are subsequent acceptance steps after exact-head CI.
